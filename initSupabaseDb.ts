import { insertMultiplePOIs } from './services/realDatabaseService';\nimport { CAMPUS_POIS } from './data/campusPOIs';\n\nconst initializeSupabaseDatabase = async () => {\n  try {\n    console.log('Initializing Supabase database...');\n    \n    await insertMultiplePOIs(CAMPUS_POIS);\n    \n    console.log('✅ Supabase database initialized successfully!');\n    console.log('📊 Inserted', CAMPUS_POIS.length, 'POIs');\n    process.exit(0);\n  } catch (error) {\n    console.error('❌ Database initialization failed:', error);\n    console.log('\\n🔧 Make sure to:');\n    console.log('1. Create a Supabase account at https://supabase.com');\n    console.log('2. Create a new project');\n    console.log('3. Add your SUPABASE_URL and SUPABASE_ANON_KEY to .env.local');\n    console.log('4. Create the campus_pois table in your Supabase dashboard');\n    process.exit(1);\n  }\n};\n\ninitializeSupabaseDatabase();